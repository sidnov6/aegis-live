# Point the UI at the backend. Defaults to local dev if unset.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
# WS URL is derived from API_BASE by default; override only if different host.
# NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8000/ws
